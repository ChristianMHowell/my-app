import React from "react"

export interface IStoreProps{

}
export const mainStore: React.FunctionComponent<IStoreProps> = props => {

    return (
        <React.Fragment>

            
        </React.Fragment>
    )
}